import { AppStateInterface } from 'src/app/shared/types/appState.interface';
import { AuthCashStateInterface } from 'src/app/auth-cash/types/authCashState.interface';

/*export const authFeatureSelector = createFeatureSelector<AppStateInterface, AuthCashStateInterface>('authCash');

export const isSubmittingSelector = createSelector(
	authFeatureSelector,
	(authState: AuthCashStateInterface) => authState.isSubmitting,
);

export const validationErrorsSelector = createSelector(
	authFeatureSelector,
	(authState: AuthCashStateInterface) => authState.validationErrors,
);

export const isLoggedInSelector = createSelector(
	authFeatureSelector,
	(authState: AuthCashStateInterface) => authState.isLoggedIn,
);

export const isAnonymousSelector = createSelector(
	authFeatureSelector,
	(authState: AuthCashStateInterface) => authState.isLoggedIn === false,
);

export const currentUserSelector = createSelector(
	authFeatureSelector,
	(authState: AuthCashStateInterface) => authState.currentUserCash,
);*/
